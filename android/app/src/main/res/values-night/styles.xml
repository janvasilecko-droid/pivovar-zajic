<?xml version="1.0" encoding="utf-8"?>
<!-- 🌙 Tmavý režim telefonu.
     ==========================================================
     Úvodní obrazovka musí vypadat STEJNĚ jako ve světlém režimu: znak
     pivovaru na bílé. Bez tohoto souboru si `Theme.SplashScreen` v tmavém
     režimu bere systémový (tmavý) podklad, takže zajíc svítí v černém
     čtverci — přesně jak přišlo z provozu 23. 9. 2026: „jako první se
     zobrazí rozmazaný logo pivovaru v černém čtverci".

     Hodnoty jsou schválně vypsané znovu, ne zděděné: dědí se jen to, co
     tady není, a právě podklad je to, co tmavý režim mění. -->
<resources>
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/splash_background</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
    </style>
</resources>
