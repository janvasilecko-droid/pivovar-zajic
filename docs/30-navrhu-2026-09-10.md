# 30 návrhů — 10. 9. 2026

Uživatel řekl „udelej vse" k seznamu níže. Postup se zapisuje sem průběžně —
u položky, kde se po prozkoumání ukáže něco jiného, než tvrdil návrh, se to
opraví přímo v zápisu (stejná zásada jako u `20-navrhu-2026-09-08.md`).

## WhatsApp a objednávky

1. ✅ HOTOVO — UŽ BYLO. Dead-letter fronta pro zprávy, které se nepodařilo
   předat webhooku, existuje (`whatsapp_neodeslane`, `whatsapp-bridge/lib/webhook.js`
   — `odloZpravu`/`posliOdlozene`, retry po připojení a pravidelně) a je
   vidět v „hloubkovém auditu" (`kontrolaFronty` v `lib/hloubkovyAudit.ts`).
   Tichá díra popsaná v paměti byla zacelena v dřívější session.
2. ✅ HOTOVO — UŽ BYLO. `lib/whatsappTicho.ts` + banner na Domů (`ticho.varovat`)
   už detekuje, když most dlouho nic nedoručil, a ukáže kdy naposledy a
   odkaz na spárování QR kódem.
3. ✅ HOTOVO. V detailu objednávky (blok „Původní WhatsApp zpráva",
   `Orders.tsx`) přibylo tlačítko „Zkusit znovu najít odběratele v téhle
   zprávě" — zavolá znovu AI parsování textu, najde/založí odběratele,
   uloží ho k objednávce a naučí alias. Funguje i na staré objednávky,
   které skončily jako „Neznámý odběratel" před dnešní opravou promptu.
4. ✅ HOTOVO. Nová sekce „Naučení odběratelé" v Nastavení (jen pro admina)
   — seznam všech `place_aliases` (špatný text → odběratel, kolikrát použito,
   naposledy) a tlačítko na smazání špatného naučení bez SQL.
5. ✅ HOTOVO — ale jinde, než tvrdil návrh. Skutečný problém nebyl v
   klientském `matchPlaceFromText` (ten správně slouží jen k porovnání s
   existujícím katalogem), ale ve `supabase/functions/parse-order-image`
   (čtení z fotky): nalezen poškozený/zdvojený blok pravidel pro odběratele
   — pozůstatek špatného mergu (utržená věta uprostřed nové nadpisu, dvě
   různá pravidlo č. 4). Opraveno a doplněny stejné dvě věci jako dnes ráno
   v `parse-order-text`: „místo/adresa je taky odběratel" a obecný vzorec
   „nevzdávej se na null, hledej jméno v textu". Nasazeno.
6. ❌ PROVĚŘENO A ZAMÍTNUTO — UŽ BYLO HOTOVO JINAK. Kompaktní přehled neshod
   čtení existuje (`WhatsAppOrderReviewModal`): skóre důvěryhodnosti,
   počty sedí/částečně/nesedí a zvýraznění konkrétních řádků, které
   nesouhlasí. Návrh vycházel z mylného předpokladu, že je vidět jen počet.

## Sklad a inventura

7. ❌ PROVĚŘENO A ZAMÍTNUTO. Appka nemá žádné nezávislé měření skutečného
   objemu v tanku (žádný senzor) — „kontrola shody" by jen porovnávala
   vlastní účetnictví appky samo se sebou a nikdy by nezachytila reálný
   rozdíl jako u Spilky 1 / Tanku 6. To musí změřit člověk.
8. ❌ PROVĚŘENO A ZAMÍTNUTO — UŽ TO TAK FUNGUJE. Dlaždice „Inventura" na
   Domů už vede jedním kliknutím na obrazovku Inventura, která se sama
   otevírá na měsíci, jenž se uzavírá, s tlačítkem „Schválit & převést do
   nového měsíce" rovnou nahoře. Zpoždění u srpnové inventury bylo lidské
   (nikdo neklikl), ne chybějící krok v UI.
9. ⏸ ODLOŽENO. Křížová kontrola vrácených sudů mezi Výčepy a Fasováním
   zasahuje do dvou subsystémů skladového účetnictví — bez hlubšího
   prozkoumání obou toků je riziko, že se udělá chyba v datech, které
   se pak špatně dohledává. Chce to vlastní prostor, ne rychlou úpravu
   v rámci třicítky bodů.
10. ⏸ ODLOŽENO. Sklad se zapisuje z mnoha míst po appce (Stock, Cellar,
    Fasování, Prodejna…) — bezpečné pokrytí audit logem všech cest by
    bylo srovnatelně velké jako celá tahle třicítka bodů zvlášť.

## Potřeby stáčení / KEG / Lahve

11. ⏸ ODLOŽENO — jinak, než tvrdil návrh. Týdenní souhrnný pohled UŽ
    EXISTUJE — jen na jiné obrazovce („Potřeby stáčení" v Nastavení, admin),
    ne na dennním zápisu KEG/Lahve. Denní zápis má záměrně jiný účel
    (zapsat, co se dnes fakticky stočilo) a přidávat mu týdenní pohled by
    bylo riziko v čerstvě (dnes) stabilizovaném kódu bez jasného přínosu.
12. ❌ PROVĚŘENO A ZAMÍTNUTO — UŽ BYLO HOTOVO. „Potřeby stáčení" u každé
    položky píše „sklad X · fasování odhad Y" a v patičce vysvětluje přesný
    vzorec („odhad fasování = průměr za posledních 30 dní"). Návrh
    vycházel z mylného předpokladu tichého zaokrouhlení bez vysvětlení.
13. ✅ OVĚŘENO, ŽÁDNÝ PROBLÉM. Kalkulačky a Export do Excelu tabulku
    `bottling_plans` vůbec nečtou. HomeScreen (odznak) a Calendar (tečka
    v kalendáři) čtou jen počet/datum — rozdělení jednoho řádku na dva
    (dnešní oprava lahve+KEG) je tam neškodné, spíš přesnější.
14. ⏸ ODLOŽENO. Záznam kdo/kdy označil „nachystáno" je nová funkce (vlastní
    audit trail), ne jen zpřístupnění existujícího — mimo rozsah tohohle kola.

## Push upozornění

15. ✅ HOTOVO. `whatsapp-webhook` po uložení nové zprávy (mimo vlastní/from_me)
    pošle push „WhatsApp: <odesílatel>" přes `posli-push`, ověřeno interním
    cron secretem z `app_secrets` (stejný vzorec jako `whatsapp-auto-parse-poll`).
16. a 17. ✅ HOTOVO — spojeno do jednoho. „Výčep po termínu" nemá vlastní
    okamžik „teď se to stalo" (je to stav, ne událost), takže dává smysl
    jen pravidelná kontrola — proto oba body dohromady jako jeden ranní
    souhrnný push (migrace `20261230010000`, cron denně v 5:00 UTC):
    kolik WhatsApp objednávek čeká na schválení + kolik výčepů je po
    termínu (a za kolik kaucí). Posílá se, jen když je co hlásit.
    **Migrace čeká na ruční spuštění uživatelem** (Nastavení → Diagnostika
    → Databázové migrace), appka je sama nespouští.
18. ✅ HOTOVO. Tlačítko „Odeslat zkušební upozornění" v Nastavení (u
    přepínače push) — appka si sama ověří, že server umí notifikace
    odeslat, a ukáže srozumitelný výsledek místo tichého 503.

## Mobilní ovladatelnost

19.–21. ✅ OVĚŘENO. Proměřeno v prohlížeči na 375 px (Domů, Potřeby
    stáčení): žádné vodorovné přetečení stránky na žádné z obou obrazovek.
    Nalezeny jen 2–3 tlačítka o rozměru 34–36 px místo doporučených 44 px
    (navigační šipky týdne) — drobné, nekritické, neopravováno bez jistoty,
    co přesně to je (riziko zásahu naslepo). Nepodařilo se ověřit přímo
    +/- steppery v otevřeném modálu „Stočit" (opakovaný timeout nástroje
    prohlížeče, ne appky) — jejich rozměr (`w-9 h-9` = 36×36 px) je vidět
    v kódu a je stejný jako jinde v appce.

## Grafika a sjednocení

22. ⏸ ODLOŽENO — stejný důvod jako 8.–9. 9. 2026 (`20-navrhu-2026-09-08.md`,
    bod 4). Sjednocení nadpisů napříč ~40 obrazovkami je větší úloha, ne
    čtvrtinový bod v rámci třicítky.
23. ❌ ZAMÍTNUTO. `npm test` (včetně `jednotnyVzhled.test.ts`, který dnes
    odhalil 36 chyb barev) běží v CI PŘED deployem a blokuje ho při pádu
    (`.github/workflows/deploy.yml`) — reálná ochrana proti tomu, aby
    `red-*` skončilo na produkci, už existuje. Chybí jen kontrola PŘED
    commitem (pre-commit hook), což by vyžadovalo novou infrastrukturu
    (husky/lint-staged) pro relativně vzácnou chybu — poměr přínos/náklad
    to nevychází.
24. ⏸ ODLOŽENO. Sdílená komponenta `QtyStepper` je čistě úklidový refaktor
    bez uživatelské hodnoty, s rizikem zásahu do dnes stabilizovaného kódu
    (Kegging/BottlingScreen/BottlingPlanPlanner) — nedělat bez důvodu.

## Zavoz a doprava

25. ❌ PROVĚŘENO A ZAMÍTNUTO — UŽ BYLO HOTOVO. Kniha jízd už umí
    „auto-generovat" trasu přímo ze skutečných objednávek daného měsíce
    (i s rozdělením dne na dvě jízdy, když část vezla Kačena a část velké
    auto) — `KnihaJizdScreen.tsx`. Návrh vycházel z mylného předpokladu.
26. ✅ HOTOVO. V modálu podpisu převzetí (`PodpisModal`, použito v Závozu)
    přibyla možnost „Vyfoť doklad nebo vratku" jako alternativa/doplněk
    k podpisu — fotka se ukládá k objednávce stejně jako fotky poškozeného
    zboží v detailu objednávky (`FotkyZaznamu`).
27. ⏸ ODLOŽENO. Ověřeno přímo v datech: jen 19 z 56 odběratelů (34 %) má
    vyplněné GPS souřadnice. Řazení zastávek podle vzdálenosti by fungovalo
    jen třetině z nich a zbytek by nechávalo v nejasném pořadí — blokováno
    na datech (chybějící souřadnice), ne na kódu.

## Ostatní obrazovky

28. ⏸ ODLOŽENO. Kód sám (`lib/hodnotaObjednavky.ts`) už dokumentuje, že
    „fakturace a měsíční vyúčtování je samostatná, mnohem větší úloha,
    kterou nejde uhodnout bez toho, jak pivovar doopravdy fakturuje" —
    tahle hranice byla vědomě nastavená dřív a nemá smysl ji obcházet
    napůl promyšleným reportem v Exportu, který má navíc úplně jinou
    datovou architekturu (výdeje/stáčení, ne objednávky × ceník).
29. ⏸ ODLOŽENO. Propojení termínů sanitace s připomínkami vyžaduje pochopení
    domény (jaké termíny se sledují, jak se opakují), které jde odhadnout
    špatně — riziko chybějící/otravné připomínky u HACCP tématu je vyšší
    než u běžné UI úpravy. Chce to ověřit s uživatelem, ne jen naprogramovat.
30. ✅ HOTOVO — menší, než tvrdil návrh. Přehled zpětné vazby PRO ADMINA
    už existoval (`Feedback.tsx` — filtr podle kategorie, seskupení podle
    stavu, stavy Nové/V řešení/Hotovo/Zamítnuto). Přidán jen chybějící
    kompaktní souhrn počtů nahoře („3× Nové · 1× V řešení…"), ať je to
    vidět na první pohled bez scrollování.

## Shrnutí

- **Nový kód dnes:** body 3, 4, 5, 15, 16+17, 18, 26, 30 (8 věcí).
- **Už bylo hotové, jen nebylo vidět z návrhu:** body 1, 2, 6, 8, 12, 13, 25 (7 věcí).
- **Proměřeno/ověřeno bez potřeby zásahu:** body 19.–21.
- **Zamítnuto s důvodem:** body 7, 23.
- **Odloženo s důvodem:** body 9, 10, 11, 14, 22, 24, 27, 28, 29.

Testy: `npx vitest run` 150 souborů / 1611 testů zelených, `tsc --noEmit`
bez chyb, `npm run build` prošel. Edge funkce (`parse-order-text`,
`parse-order-image`, `whatsapp-webhook`) nasazené ručně přes
`supabase functions deploy` (CI je nepokrývá). Migrace
`20261230010000_ranni_souhrn_a_push_pri_objednavce.sql` čeká na ruční
spuštění uživatelem.
