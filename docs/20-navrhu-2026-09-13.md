# 20 návrhů — 13. 9. 2026 (páté kolo)

Návrh vznikl z kódu na `origin/main` (appka v prohlížeči nebyla přihlášená).
Uživatel řekl „19 ne, jinak všechno udělej". Stav každého bodu níž.

Předtím se ve stejný den dokončilo a nasadilo **čtvrté kolo** (bezpečnost,
`docs/50-navrhu-2026-09-13.md`) — do té doby leželo jen neuložené v pracovní
kopii a na produkci nebylo.

## Data a zálohy

1. **✅ Hotovo (šifrovaně).** Repozitář je veřejný a záloha v něm byla
   čitelná. Uživatel zvolil šifrování: AES-256-GCM, heslo v secretu
   `ZALOHA_HESLO` (`scripts/lib/sifraZalohy.mjs`). Zálohuje se 68 tabulek
   místo 4; vynechané jsou tajemství a provozní logy
   (`scripts/lib/zalohaTabulky.mjs`). Obnova ve dvou kolech kvůli kruhu
   objednávky ↔ WhatsApp. Ověřeno nanečisto proti produkci (2 497 řádků,
   „databáze se zálohou souhlasí"). **Bez secretu `ZALOHA_HESLO` se záloha
   nezapíše vůbec.** Staré nešifrované zálohy zůstávají v historii gitu.
2. **✅ Hotovo.** Hlídač noční zálohy: Diagnostika a odznak na dlaždici
   Záloha na Domů (admin), GitHub issue při selhání (`lib/zalohaStav.ts`).
3. **✅ Hotovo.** Migrace z CI: `scripts/pust-cekajici-migrace.mjs` + job
   `nasad-databazi-a-funkce` v `deploy.yml`. Pojistky: jen migrace novější
   než evidence, po řadě, stop při chybě, mazací migrace (DROP TABLE,
   TRUNCATE, DROP COLUMN, DELETE bez WHERE) se automaticky nepustí.
   **Vypnuté, dokud** v repozitáři není secret `SUPABASE_ACCESS_TOKEN`
   a proměnná `AUTO_MIGRACE=ano` — bez nich job jen vypíše, co čeká.
4. **✅ Hotovo.** Edge funkce z CI — ve stejném jobu, nasazují se jen
   změněné (změna v `_shared/` = všechny). Stejný secret jako bod 3.
5. **✅ Hotovo jinak, než znělo.** Testovací účet by k ověřování nepomohl
   (Claude se do appky přihlašovat nesmí). Místo toho náhled bez přihlášení
   umí celé části obrazovek: `nahled/obrazovky.html` s podstrčenou
   Supabase i auth (`vite.nahled.config.ts`). Zatím Várky a Ztráty —
   přesně takhle se ověřily a našla se v nich chyba (desetinná tečka místo
   čárky). Spuštění: `.claude/launch.json` → „nahled" (port 5198).

## Sudy, sklad, výroba

6. **✅ Hotovo.** Závoz → Konto sudů ukazuje „X dní bez vrácení" a počet
   odběratelů přes 30 dní (`lib/sudyVenku.ts`).
7. **❌ Už existovalo.** „Za kolik dní dojde" počítá Sklad od dřív
   (`lib/predpovedDojiti.ts`). Návrh vycházel z chybného hledání.
8. **✅ Hotovo.** Trvanlivost lahví: pole u piva (Katalogy), Sklad u lahví
   píše „MT za X dní" / „MT prošlo" podle nejstarší šarže, která na skladě
   nejspíš leží (FIFO odhad, `lib/trvanlivostSarzi.ts`). Migrace
   `20261231060000`.
9. **✅ Hotovo.** Sklep → Várky & kvašení: průběžná stupňovitost a teplota,
   graf s časovou osou, prokvašení. Tabulka `cellar_batches` existovala od
   srpna, ale **nic do ní nepsalo**. Migrace `20261231050000`.
10. **✅ Hotovo.** Sklep → Ztráty při stáčení: vážený průměr po pivech
    a tancích, vývoj posledních 3 cyklů proti starším (`lib/ztratyTanku.ts`).
11. **✅ Hotovo.** Generace kvasnic u várky + z které várky pochází
    (nová generace se předvyplní o jedna vyšší).

## Upozornění

12. **✅ Hotovo.** Ranní souhrn navíc hlásí sudy přes 30 dní venku
    a končící STK/dálniční známku. **Docházející zboží záměrně ne** — stav
    skladu počítá skladová kniha v appce z 12 tabulek a druhá kopie v SQL
    by se rozešla. Migrace `20261231030000`.
13. **✅ Hotovo.** Středeční push o pravidelných odběratelích, kteří mlčí
    (stejná pravidla jako `lib/kontrolaObjednavek.ts`). Stejná migrace.

## HACCP a doklady

14. **✅ Hotovo.** Sanitace → export všech čtyř deníků do Excelu za období.
15. **❌ Už existovalo.** Tisk závozového listu umí Závoz i jeho historie
    (`lib/safePrint.ts`).

## Stabilita kódu

16. **✅ Hotovo (Objednávky).** `screens/Orders.tsx` 4 114 → 2 916 řádků;
    karta, detail, souhrn variant a WhatsApp blok jsou v
    `components/objednavky/`. Testy, které četly text z `Orders.tsx`,
    míří na nový soubor; hlídka barev písma pokrývá i nové soubory.
    Domů a Inventura zůstaly — jedna velká komponenta bez vnitřních
    podkomponent, rozdělení by byl přepis, ne přesun.
17. **✅ Hotovo.** `src/lib/database.types.ts` z `supabase gen types`,
    `npm run typy-db`, `lib/dbTypy.ts` (`Radek<'tabulka'>`). `backup.ts`
    bez `any`. Klient Supabase typovaný zatím není (typy odrážejí produkci,
    kde ještě neběží dnešní migrace).
18. **✅ Hotovo.** Měsíční zkouška obnovy (`zkouska-obnovy.yml`): rozšifrovat,
    porovnat s manifestem, zkontrolovat návaznosti a nahrát do prázdné
    dočasné PostgreSQL v Actions. Issue při selhání.

## Pohodlí

19. **Vynecháno** (uživatel nechtěl).
20. **✅ Hotovo.** Ceník → Historie změn cen. Zapisují triggery, takže
    zná i změny mimo obrazovku. Migrace `20261231040000`.

## Nalezeno cestou

- **`analyze-reviews` se nedala nasadit** — `\\`` v promptu ukončoval
  šablonový řetězec. Opraveno a nasazeno.
- Lokálně vitest nenačte `.mjs` skript přímo (SyntaxError, v CI prošlo);
  test skriptu migrací ho proto volá přes `node`.
- Na portu 5199 zůstal viset starší server náhledu (spuštěný 12:05) —
  proto náhled v `launch.json` běží na 5198.

## Čeká na uživatele

- **Spustit migrace** (Nastavení → Diagnostika → Databázové migrace):
  tři ze čtvrtého kola (`20261230060000`–`80000`) a čtyři z pátého
  (`20261231030000`–`60000`).
- Volitelně zapnout automatické migrace a edge funkce: v GitHubu přidat
  secret `SUPABASE_ACCESS_TOKEN` a proměnnou `AUTO_MIGRACE=ano`.
- Rozhodnout o veřejné záloze (body 1, 2, 18).
