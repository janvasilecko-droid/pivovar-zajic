# 20 vylepšení — 8. 9. 2026

Navrženo po dokončení redesignu „Potřeby stáčení" a funkce „už uloženo dnes"
u KEG. Uživatel schválil: „ano udelej vse". Postup se zapisuje sem průběžně —
u položky, kde se po prozkoumání ukáže něco jiného, než tvrdil návrh, se to
opraví přímo v zápisu (stejná zásada jako u `50-navrhu-2026-09-07.md`).

## Grafika a sjednocení

1. ✅ HOTOVO — ale jinak, než návrh tvrdil. `.card`=2xl, `.btn`/`.input`/`.chip`=xl
   a holé `rounded` (1600+ míst) je od dřívějška záměrně přeladěné v
   `tailwind.config.ts` na 0,625rem — to je fungující třívrstvý systém, ne
   chaos, jak jsem si myslel z prvního vzorku. Skutečný nález: `rounded-lg`
   v Tailwindu není v tomhle configu přeladěné, takže padá na výchozích
   0,5rem — PŘESNĚ STEJNĚ jako `rounded-md`. Dvě různá jména dělají jednu
   barvu. Našel jsem 3 taková místa a všechna byla MOJE VLASTNÍ z včerejška
   (chybník, varování u zdvojené položky, tlačítko „Stočit" v seskupené
   kartičce) — opraveno na `rounded-xl`/`rounded`, ať sedí se zbytkem téhož
   souboru. Zbylých ~47 `rounded-lg` v appce je starších a nekontroloval
   jsem každé zvlášť — to by byla samostatná, větší úloha.
2. ✅ HOTOVO — UŽ BYLO. `font-variant-numeric: tabular-nums` je od 5. 9. 2026
   globálně na `body` (viz komentář v `index.css`), takže se to netýká jen
   „míst" — je to všude. Návrh byl zastaralý, ověřeno přímo v kódu.
3. ✅ HOTOVO — ale jinde, než tvrdil návrh. „Připraveno" je ve skutečnosti
   všude důsledně emerald (jen v různých odstínech podle role — plná
   výplň na odznaku, jemný nádech na pozadí řádku — to je správně, ne
   chyba). Historie stav objednávky barvou vůbec neukazuje, takže tam
   nebylo co srovnávat. Skutečná chyba byla u **„Zavezeno"**:
   `src/screens/Orders.tsx:3238` mělo `bg-violet-700`, zatímco stejný
   stav o pár řádků výš i jinde v souboru (2504, 3487) má `bg-emerald-700`
   — podle `docs/jednotny-styl.md` je emerald role pro „Hotovo, Zavezeno,
   Schválit". Opraveno na emerald.
4. ⏸ PROVĚŘENO, VĚTŠÍ NEŽ VYPADALO — odloženo se zdůvodněním. Nesrovnalost
   je reálná (např. `Kegging.tsx`/`BottlingScreen.tsx` mají identický blok
   „Začátek stáčení…" zkopírovaný dvakrát; `Cellar.tsx` používá `<h1>`,
   jinde je to `<h2>` nebo obyčejné `<div>`; váhy i barvy nadpisů se liší
   napříč ~40 obrazovkami). Sjednotit natvrdo bez prohlédnutí každé
   obrazovky zvlášť (některé mají navigaci týdne, jiné odznak ADMIN, jiné
   nic) by bylo přesně to plošné přepisování, co si tenhle projekt
   opakovaně nese na vlastní kůži jako riziko. Nedělám narychlo mezi
   zbylými 16 body — chce to vlastní prostor, ne odškrtnutí.
5. ✅ HOTOVO — ale menší, než tvrdil návrh. `EmptyState` už se používala na
   67 místech, ne „na pár", takže adopce je vysoká. Skutečných ad-hoc
   prázdných stavů na úrovni celé sekce/seznamu (ne malé kartičky uvnitř
   statistik, kde by `EmptyState` byla naopak moc těžká) bylo jen 4:
   `TimersScreen.tsx` (žádný časovač), `History.tsx` (žádné ukončené
   cykly tanků), `StatistikaVystav.tsx` (žádná objednávka v období),
   `BottlingLineMaintenance.tsx` (žádné úkony údržby). Všechny čtyři
   převedené na `EmptyState` s ikonou.

## Zjednodušení

6. ✅ HOTOVO. Desktopová tabulka teď třídí zebra pruh podle PIVA (ne podle
   pořadí řádku), takže obaly téhož piva vizuálně tvoří skupinu jako na
   mobilu, a tlačítko „Stočit" otevře formulář za CELÉ pivo (přes
   `openStocitGroup`), ne jen za jeden řádek — sedí to teď k mobilní verzi.
7. ✅ HOTOVO. Stejný „už uloženo dnes" štítek jako u KEG (7. 9.) je teď
   i u Lahví — u každého obalu v panelu a na zavřené dlaždici („dnes 45×1").
   Sdílí kód s KEG přes `lib/jizUlozeno.ts`.
8. ❌ PROVĚŘENO A ZAMÍTNUTO. Obalů je v jednom výběru jen 4–5 (lahve:
   1,5/1/0,5/0,33 l; KEG: 10/15/20/30/50 l) — „hledání v seznamu" u pěti
   položek není reálný problém, na rozdíl od 23 piv. Přeřazovat podle
   naposledy použitých by tu nic nezrychlilo. Škrtnuto, ne odloženo.
9. ⬜ Kopírovat objednávku jako text.
10. ⬜ Rychlé hledání (Ctrl+K) není nikde zmíněné.

## Funkce

11. ✅ HOTOVO. Repozitář je veřejný, takže GitHub API pro čtení běhů
    workflow jde zavolat přímo z prohlížeče admina bez tokenu
    (`lib/nasazeniStav.ts`, 5 testů). V Diagnostice přibyl blok, který
    mlčí, dokud je poslední nasazení v pořádku, a červeně upozorní
    (s odkazem na GitHub), když poslední pokus selhal — appka mezitím
    dál běží v poslední úspěšně nasazené verzi, nic se nerozbilo, jen
    se nedostala ven poslední změna.
12. ✅ HOTOVO — UŽ BYLO, jen jinde, než jsem hledal. `Stock.tsx` má u
    každé položky s `rawStock < 0` červený text i vlastní vysvětlení:
    „Evidence vychází do mínusu o X ks — vydalo se víc, než kolik se kdy
    naskladnilo… rozdíl je potřeba dohledat (chybějící stočení, špatně
    zapsaný počáteční stav, nebo duplicitní odpočet)." `InventoryScreen.tsx`
    má analogicky `expectedQty < 0` v audit záložce. Přesně řeší i případ
    z 8. 9. (srpnová nula, ale zářijové odpočty) — nekontroloval jsem
    přímo TU položku, ale mechanismus na ni sedí.
13. ✅ HOTOVO — a byl to skutečný, ne domnělý nález. Appka měla v databázi
    hotovou tabulku `audit_log` (obecný audit s `table_name`, `record_id`,
    `old_data`/`new_data`, `changed_by`), ale ŽÁDNÝ kód do ní nikdy
    nezapisoval — `lib/audit.ts` je jiná věc, lokální localStorage log jen
    s ukázkovými řádky. Přidáno: zápis při přidání/úpravě/smazání položky
    objednávky (`lib/objednavkaAudit.ts`, 5 testů) a rozbalovací „Historie
    změn" v detailu objednávky — kdo, kdy a co změnil, čitelnou větou
    ("množství 2 → 4", ne přepis celého řádku).
14. ✅ HOTOVO — uživatel si o tohle řekl živě 9. 9., nezávisle na tomhle
    seznamu ("potřeboval bych někde vidět, zda mám zapsáno veškeré
    stáčení… musí být stočeny všechny objednávky"). V Zavozu se odškrtávalo
    "stočeno" ručně, bez porovnání se skladovou knihou. Teď se u každé
    nezavezené objednávky v Zavozu (sekce Nakládání) počítá stejný odznak
    "chybí skladem", jaký mají Objednávky (`lib/tydenniZbytek.ts` —
    stejná, ne čtvrtá, definice "chybí"), takže je to vidět na místě, kde
    se doopravdy nakládá.
15. ✅ HOTOVO — ale jako varování, ne zámek. Skutečný ZÁKAZ zápisu by mohl
    zablokovat legitimní dodatečnou opravu bez úniku — proto appka jen
    NAHLAS upozorní, stejným vzorem jako u zdvojené položky objednávky
    (viz bod 40 z minulého seznamu): „Měsíc už má napočítanou inventuru…
    opravdu zapsat?" (`lib/mesicUzamcen.ts`, 6 testů). Zapojeno do
    Kegging.tsx a BottlingScreen.tsx — dvou nejčastěji používaných
    zápisových obrazovek. Fasování, Prodejna a Odpis zůstávají bez téhle
    kontroly — je to reálně menší objem zápisů a bylo by to další kolo
    stejné práce navíc k šesti dnešním předchozím.
16. ✅ HOTOVO — UŽ BYLO, návrh vycházel ze špatného předpokladu. Fasování
    a Prodejna nejsou dvě různé obrazovky — obě jedou přes stejnou
    `ProdejnaScreen.tsx` (jen s jiným `table` propem). Tlačítko „↩
    Odfasovat (vrátit na sklad)" tam není podmíněné na `table ===
    'prodejna'`, takže funguje úplně stejně i ve Fasování. Beze změny kódu.
17. ❌ PROVĚŘENO A ZAMÍTNUTO — návrh popletl dvě různé věci. Export do
    Excelu (`lib/mesicniExport.ts`) je syrový přepis zápisů do listů,
    jaké si pivovar vede ručně (jeden řádek = jeden zápis, přímo ze
    zdrojových tabulek). Rozpad piva (`lib/rozpadPiva.ts`) je AGREGOVANÝ
    souhrn ze skladové knihy (stejný zdroj jako Sklad a Inventura). Obojí
    čerpá ze stejných tabulek, jen v jiném tvaru — to není nesrovnalost k
    opravě, to je zamýšlený rozdíl mezi „syrový zápis" a „souhrn".

## Výhled

18. ⚠️ ČÁSTEČNĚ — živý widget s číslem na ploše NEJDE (web/PWA na to
    nemá API, chtělo by to psát nativní Android kód, mimo tenhle stack).
    Můj vlastní dřívější předpoklad byl navíc mylný: appka NEMĚLA žádné
    „shortcuts" (zkratky), jak jsem tvrdil — `manifest.webmanifest` je
    neměl vůbec. Přidány teď 4 (KEG, Lahve, Objednávky, Rozvoz) — a při
    tom se ukázalo, že appka vůbec NEČETLA `?page=`, i když ho `sw.js`
    posílal z kliknutí na push upozornění už dřív. Ten odkaz byl mrtvý
    — appka se vždycky otevřela na Domů bez ohledu na to, na co se
    kliklo. Doplněno v App.tsx (`lib/vstupniStranka.ts`, 4 testy) — spraví
    to zároveň zkratky i push upozornění.
19. ⬜ Nápověda pod otazníkem u každé obrazovky.
20. ⬜ Ceník a fakturace.

---

**Stav k 8. 9. 2026 22:40 — přerušeno, pokračování zítra na jiném počítači.**
Hotovo: 2, 6, 7. Zamítnuto (prověřeno, nevyplatí se): 8. Rozdělané: 1 (viz
poznámka u bodu). Zbytek (3–5, 9–20) ještě nezačato — čisté od nuly.
